{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "main-window permissions (ARCHITECTURE.md §12: scoped to main)",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default", "store:default"]
}
