# StudyVis audit ledger — branch `audit/sev1-sev2-fixes`

Baseline before audit: `tsc -b`, `eslint`, `vitest` (312), `cargo test/fmt/clippy`, `vite build`, `cargo check --locked` all green. So all findings are logic / correctness / security, not build failures.

Severity: Sev1 = data loss / security hole / crash / broken core flow. Sev2 = incorrect behavior or spec violation with real user impact. Sev3 = minor. Sev4 = nit.

| ID  | Sev                      | Location                                                                                                         | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                              | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | ------------------------ | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I1  | Sev1                     | `src/features/session/pomodoro.ts:357-373`                                                                       | `stop()` sends no wire message; receivers' 10 s silence timer fires `onSilenceExpired` → `pickNextBroadcaster` resumes the timer under a new broadcaster ~10 s after the user clicked Stop. The code comment claims receivers infer end via the `pomodoro_end` audit event, but the controller never consumes audit events.                                                                                                                           | **fixed**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| I2  | Sev2                     | `src/features/friends/presence.ts:97-104,51-60`                                                                  | Online state stores the _sender's_ `Date.now()` (`data.ts`) and compares it against the _receiver's_ `now`. Cross-host wall clocks are not comparable → persistent false-offline/online under ordinary skew. The `previous >= ts` guard wedges presence permanently if the sender's clock ever steps backward (NTP/DST). Breaks the §6 step-0 reachability signal.                                                                                    | **fixed**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| I3  | Sev2                     | `src/features/session/lifecycle.ts:194-201` + `src/stores/sessionStore.ts:166-217`                               | On the normal everyone-else-leaves auto-end path (ARCHITECTURE §13 "peer count drops to 1 → report"), `peerLeft` deletes each peer (incl. `edPubkeyHex`) before `buildLeaveHandler` snapshots. `collectPeerPubkeys()` then returns `null` and `markStudied([])` is a no-op → `sessions.peer_pubkeys` persisted NULL and friends' "last-studied-with" date never updates. Store comment already states the intent is "every ed_pubkey we've observed". | **fixed**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| I4  | Sev2                     | `src/features/ai/benchmark.ts:246-268,217-218`                                                                   | `summariseBenchmark` p95 = max of 3 samples = the cold-start warmup sample (file's own comment: warmup ≈60–90 s vs steady 15–30 s). `sampleIntervalSec = max(5, ceil(p95+1))` ⇒ AI samples ~5–10× too slowly; `sampleLoop.effectiveIntervalSec` clamps the user override to `max(modelFloor, override)` so the user cannot recover. Violates PLAN §5 "model picker accurately predicts speed".                                                        | **fixed**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| I5  | Sev2                     | `src-tauri/src/commands/models.rs:453`                                                                           | Resume fast-path calls synchronous `hash_file_blocking` over a multi-GB GGUF directly inside `async fn run_download` (no `.await` yield), pinning a Tauri async-runtime worker for seconds and stalling concurrent IPC. Sibling code (`sidecar.rs:405`) already uses `spawn_blocking` for the same class of work.                                                                                                                                     | **fixed**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| I6  | Sev3 (auditor said Sev2) | `src/features/ai/sampleLoop.ts:391-394`                                                                          | Battery-pause branch omits the §8 "show thermal-aware notice" and reschedules at the sample interval instead of 60 s.                                                                                                                                                                                                                                                                                                                                 | **deferred** — the core spec intent (auto-pause inference on battery <20 % to save power) is correctly implemented; this is a UX-feedback gap plus a benign poll spin (battery only refreshes every 60 s anyway, so behavior is unchanged, just an idle wake). A proper fix threads a new one-shot callback through `SampleLoopOptions` → `SessionView` → toast, which is a feature-shaped change disproportionate to a no-functional-impact gap; out of scope for a focused Sev1/Sev2 sweep. |
| I7  | Sev4                     | `src/features/session/invite.ts:43-47`                                                                           | Auditor flagged the idle-invite `hostSession()` path as bypassing the topic gate.                                                                                                                                                                                                                                                                                                                                                                     | **deferred (not a bug)** — verified in `src/routes/Home.tsx:101-137`: when `aiFeaturesEnabled`, `handleInvite`/`handleInviteAccepted` queue `pendingStart` and `TopicGateModal` resolves first, calling `setPendingInitialTopic` _before_ `inviteToCurrentSession` runs. `inviteToCurrentSession` has no callers other than Home. No real path reaches the bootstrap with AI on and no declared topic.                                                                                        |
| I8  | Sev3                     | `src/features/session/SessionView.tsx:362-368`                                                                   | Audit-event receive path does not check `data.session_topic === sessionTopic` (the `ai_alert` path does, `aiAlerts.ts:205`).                                                                                                                                                                                                                                                                                                                          | **deferred** — rooms are session-topic-scoped (trystero password per session) and the model is friends-only (ARCHITECTURE §14); a cross-session replay needs a paired friend deliberately misbehaving with no score benefit. Real but minor; fixing it opportunistically would violate the repo's scope-discipline house rule for a Sev3.                                                                                                                                                     |
| I9  | Sev3                     | `src/features/session/pomodoro.ts:306-335`                                                                       | Any peer sending a valid signed `pomodoro` msg is accepted as broadcaster even mid-broadcast by another.                                                                                                                                                                                                                                                                                                                                              | **deferred** — explicitly within the accepted threat model: ARCHITECTURE §14 "Friend disables their own AI / fakes score — Not defended. Social trust." Code comment already documents the deliberate "most recent sender" choice.                                                                                                                                                                                                                                                            |
| I10 | Sev3                     | spec `ARCHITECTURE.md §7:279`                                                                                    | `score_final` data-channel message type has no producer or consumer; the report is built entirely from local SQLite by design.                                                                                                                                                                                                                                                                                                                        | **deferred** — spec drift, not a code defect. House rules forbid gratuitous doc churn; the report mechanism is correct and matches PLAN §4 ("only end-of-session score and event flags shared"). Tracked here for a future doc-reconciliation pass.                                                                                                                                                                                                                                           |
| I11 | Sev3                     | `src/features/ai/sampleLoop.ts:785`                                                                              | Declared topic interpolated into the focus prompt without injection delimiters.                                                                                                                                                                                                                                                                                                                                                                       | **deferred** — friends-only threat model (PLAN §7, ARCHITECTURE §14): the only party harmed by topic-field injection is the user cheating their own score; peers see audit-event _kinds_, not topic text. Accepted residual risk.                                                                                                                                                                                                                                                             |
| I12 | Sev3                     | `src/features/ai/aiAgent.ts:287-292`                                                                             | Total JSON-parse failure echoes ≤200 chars of raw model output into the dialog bubble.                                                                                                                                                                                                                                                                                                                                                                | **deferred** — `intent:'unknown'` is inert in the deterministic break-rule layer (`break.ts`), `AiResponseBubble` renders escaped text (no `dangerouslySetInnerHTML`), and the surface is the user's own private dialog. Cosmetic at worst.                                                                                                                                                                                                                                                   |
| I13 | Sev3                     | `src-tauri/src/capabilities/default.json:5`                                                                      | `ai-dialog` window granted `notification:default`/`store:default`; §12 says permissions are main-window-scoped.                                                                                                                                                                                                                                                                                                                                       | **deferred** — defense-in-depth nit; both plugins are exercised only by main-window React code, and all webviews are first-party bundled local assets in a single-user desktop app. No privilege escalation path.                                                                                                                                                                                                                                                                             |
| I14 | Sev3                     | `src-tauri/src/db/migrations/001_initial.sql` + `migrations.rs:14-20`                                            | Bare `CREATE TABLE` (no `IF NOT EXISTS`) + no single-instance plugin ⇒ two simultaneous first-launches could both enter the migration tx and the second panics.                                                                                                                                                                                                                                                                                       | **deferred** — first-run-only, requires racing two processes before `app.db` exists, on a single-user desktop app with no shipped users. Rare; documented migration idempotency holds for the real (sequential) path. Below the Sev1/Sev2 fix bar; logged for a future hardening pass.                                                                                                                                                                                                        |
| I15 | Sev3                     | `src/stores/identityStore.ts:79-83`                                                                              | Identity commit writes keychain then `identity.json` with no rollback; a failed second write + new onboarding overwrites the keychain entry.                                                                                                                                                                                                                                                                                                          | **deferred** — narrow failure window (disk error exactly between two writes during first-run onboarding), recoverable via BIP39 by design (PLAN §7), Sev3. Out of scope for this sweep.                                                                                                                                                                                                                                                                                                       |
| I16 | Sev3                     | `src-tauri/src/commands/identity.rs:81-86`                                                                       | `fs::write` is non-atomic; a crash mid-write truncates `identity.json`.                                                                                                                                                                                                                                                                                                                                                                               | **deferred** — same class/owner as I15; Sev3, BIP39-recoverable, out of scope.                                                                                                                                                                                                                                                                                                                                                                                                                |
| I17 | Sev3                     | `src-tauri/src/db/sessions.rs:83-85`                                                                             | `started_at`/`ended_at`/`total_minutes` overwritten (not `COALESCE`d) on upsert while the file comment advertises additive upserts.                                                                                                                                                                                                                                                                                                                   | **deferred** — latent only: the sole caller always sends all three in one call, so no live data loss. Comment-vs-code drift, Sev3.                                                                                                                                                                                                                                                                                                                                                            |
| I18 | Sev4                     | `src/features/friends/pair.ts:93-135`, `src/lib/trystero/index.ts:15-18`, `src-tauri/src/commands/sidecar.rs:98` | `verifyHello` doesn't reject self-pubkey; stale selfId comment; `sidecar_start` trusts JS `model_path`.                                                                                                                                                                                                                                                                                                                                               | **deferred** — Sev4 nits; require user error or are by-design in the single-trusted-webview model.                                                                                                                                                                                                                                                                                                                                                                                            |
