diff --git a/Source/WTF/wtf/glib/Sandbox.h b/Source/WTF/wtf/glib/Sandbox.h
index 43a3f4db29d2..baf7500b1fc2 100644
--- a/Source/WTF/wtf/glib/Sandbox.h
+++ b/Source/WTF/wtf/glib/Sandbox.h
@@ -22,6 +22,10 @@
 
 namespace WTF {
 
+#if ENABLE(BUBBLEWRAP_SANDBOX)
+WTF_EXPORT_PRIVATE const CString& bubblewrapExecutable();
+WTF_EXPORT_PRIVATE const CString& dbusProxyExecutable();
+#endif
 WTF_EXPORT_PRIVATE bool isInsideFlatpak();
 WTF_EXPORT_PRIVATE bool isInsideUnsupportedContainer();
 WTF_EXPORT_PRIVATE bool isInsideSnap();
@@ -33,6 +37,10 @@ WTF_EXPORT_PRIVATE const CString& sandboxedUserRuntimeDirectory();
 
 } // namespace WTF
 
+#if ENABLE(BUBBLEWRAP_SANDBOX)
+using WTF::bubblewrapExecutable;
+using WTF::dbusProxyExecutable;
+#endif
 using WTF::isInsideFlatpak;
 using WTF::isInsideUnsupportedContainer;
 using WTF::isInsideSnap;
diff --git a/Source/WTF/wtf/glib/Sandbox.cpp b/Source/WTF/wtf/glib/Sandbox.cpp
index b99a94fd7f0a..e9aa1df1c48b 100644
--- a/Source/WTF/wtf/glib/Sandbox.cpp
+++ b/Source/WTF/wtf/glib/Sandbox.cpp
@@ -28,6 +28,7 @@
 #include <wtf/FileSystem.h>
 #include <wtf/NeverDestroyed.h>
 #include <wtf/glib/GRefPtr.h>
+#include <wtf/glib/GUniquePtr.h>
 #include <wtf/text/CString.h>
 
 namespace WTF {
@@ -41,6 +42,33 @@ bool isInsideFlatpak()
 }
 
 #if ENABLE(BUBBLEWRAP_SANDBOX)
+static CString packagedSandboxHelper(const char* name, const char* fallback)
+{
+    // AppImages are not chroots, so WebKit's normal /usr/bin paths refer to
+    // the host. Prefer a helper shipped beside the StudyVis executable and
+    // keep the configured path as the native-package/development fallback.
+    CString executable = FileSystem::currentExecutablePath();
+    if (!executable.isNull()) {
+        GUniquePtr<char> directory(g_path_get_dirname(executable.data()));
+        GUniquePtr<char> candidate(g_build_filename(directory.get(), name, nullptr));
+        if (g_file_test(candidate.get(), G_FILE_TEST_IS_EXECUTABLE))
+            return candidate.get();
+    }
+    return fallback;
+}
+
+const CString& bubblewrapExecutable()
+{
+    static NeverDestroyed<CString> executable = packagedSandboxHelper("bwrap", BWRAP_EXECUTABLE);
+    return executable;
+}
+
+const CString& dbusProxyExecutable()
+{
+    static NeverDestroyed<CString> executable = packagedSandboxHelper("xdg-dbus-proxy", DBUS_PROXY_EXECUTABLE);
+    return executable;
+}
+
 bool isInsideUnsupportedContainer()
 {
     static bool inContainer = g_file_test("/run/.containerenv", G_FILE_TEST_EXISTS);
@@ -51,7 +79,7 @@ bool isInsideUnsupportedContainer()
     // So this just does a basic test of if `bwrap` runs successfully.
     if (inContainer && supportedContainer == -1) {
         const char* bwrapArgs[] = {
-            BWRAP_EXECUTABLE,
+            bubblewrapExecutable().data(),
             "--ro-bind", "/", "/",
             "--proc", "/proc",
             "--dev", "/dev",
diff --git a/Source/WebKit/UIProcess/Launcher/glib/BubblewrapLauncher.cpp b/Source/WebKit/UIProcess/Launcher/glib/BubblewrapLauncher.cpp
index 67eb870f16fd..b52b78f29271 100644
--- a/Source/WebKit/UIProcess/Launcher/glib/BubblewrapLauncher.cpp
+++ b/Source/WebKit/UIProcess/Launcher/glib/BubblewrapLauncher.cpp
@@ -851,7 +851,7 @@ static void bindDBusProxyPaths(ProcessLauncher::LaunchOptions& launchOptions, Ve
 
     if (launchOptions.processType == ProcessLauncher::ProcessType::DBusProxy) {
         sandboxArgs.appendVector(Vector<CString>({
-            "--ro-bind", DBUS_PROXY_EXECUTABLE, DBUS_PROXY_EXECUTABLE,
+            "--ro-bind", dbusProxyExecutable(), dbusProxyExecutable(),
             "--bind", sandboxedUserRuntimeDirectory().data(), sandboxedUserRuntimeDirectory().data(),
         }));
 
@@ -986,7 +986,7 @@ GRefPtr<GSubprocess> bubblewrapSpawn(GSubprocessLauncher* launcher, const Proces
     g_subprocess_launcher_take_fd(launcher, bwrapFd, bwrapFd);
 
     Vector<CString> bwrapArgs = {
-        BWRAP_EXECUTABLE,
+        bubblewrapExecutable(),
         "--args",
         bwrapFdStr.get(),
         "--",
diff --git a/Source/WebKit/UIProcess/Launcher/glib/XDGDBusProxy.cpp b/Source/WebKit/UIProcess/Launcher/glib/XDGDBusProxy.cpp
index eb0c6b529ce3..f0afed5bc614 100644
--- a/Source/WebKit/UIProcess/Launcher/glib/XDGDBusProxy.cpp
+++ b/Source/WebKit/UIProcess/Launcher/glib/XDGDBusProxy.cpp
@@ -197,7 +197,7 @@ void XDGDBusProxy::launch(const CString& proxyPath, CompletionHandler<void()>&&
     int proxyFd = argumentsToFileDescriptor(proxyArgs, "dbus-proxy");
     GUniquePtr<char> proxyArgsStr(g_strdup_printf("--args=%d", proxyFd));
     Vector<CString> args = {
-        DBUS_PROXY_EXECUTABLE,
+        dbusProxyExecutable(),
         proxyArgsStr.get(),
     };
     int nargs = args.size() + 1;
diff --git a/Source/WebKit/Shared/glib/ProcessExecutablePathGLib.cpp b/Source/WebKit/Shared/glib/ProcessExecutablePathGLib.cpp
--- a/Source/WebKit/Shared/glib/ProcessExecutablePathGLib.cpp
+++ b/Source/WebKit/Shared/glib/ProcessExecutablePathGLib.cpp
@@ -29,9 +29,25 @@
 
 #include <glib.h>
 #include <wtf/FileSystem.h>
+#include <wtf/glib/GUniquePtr.h>
 
 namespace WebKit {
 
+static String packagedWebKitProcess(const char* processName)
+{
+    // AppImages are not chroots. Resolve the subprocesses in a stable
+    // directory beside the application executable before trying the native
+    // installation prefix compiled into WebKit.
+    CString executablePath = FileSystem::currentExecutablePath();
+    if (executablePath.isNull())
+        return { };
+    GUniquePtr<char> executableDirectory(g_path_get_dirname(executablePath.data()));
+    GUniquePtr<char> processPath(g_build_filename(executableDirectory.get(), "studyvis-webkit-runtime", processName, nullptr));
+    if (g_file_test(processPath.get(), G_FILE_TEST_IS_EXECUTABLE))
+        return FileSystem::stringFromFileSystemRepresentation(processPath.get());
+    return { };
+}
+
 #if ENABLE(DEVELOPER_MODE)
 static String getExecutablePath()
 {
@@ -44,6 +60,10 @@ static String getExecutablePath()
 
 static String findWebKitProcess(const char* processName)
 {
+    String packagedProcessPath = packagedWebKitProcess(processName);
+    if (!packagedProcessPath.isNull())
+        return packagedProcessPath;
+
 #if ENABLE(DEVELOPER_MODE)
     static const char* execDirectory = g_getenv("WEBKIT_EXEC_PATH");
     if (execDirectory) {
diff --git a/Source/cmake/FindRice.cmake b/Source/cmake/FindRice.cmake
--- a/Source/cmake/FindRice.cmake
+++ b/Source/cmake/FindRice.cmake
@@ -109,6 +109,7 @@ if (Rice_IO_LIBRARY AND NOT TARGET Rice::Io)
     set_target_properties(Rice::Io PROPERTIES
         IMPORTED_LOCATION "${Rice_IO_LIBRARY}"
         INTERFACE_INCLUDE_DIRECTORIES "${RiceIo_INCLUDE_DIR}"
+        INTERFACE_LINK_LIBRARIES Rice::Proto
     )
 endif ()
 
diff --git a/Source/WebKit/UIProcess/API/glib/WebKitWebContext.cpp b/Source/WebKit/UIProcess/API/glib/WebKitWebContext.cpp
--- a/Source/WebKit/UIProcess/API/glib/WebKitWebContext.cpp
+++ b/Source/WebKit/UIProcess/API/glib/WebKitWebContext.cpp
@@ -344,8 +344,27 @@ static void webkitWebContextInvalidateAutomationSession(WebKitWebContext* webCon
 #define INJECTED_BUNDLE_FILENAME "libWPEInjectedBundle.so"
 #endif
 
+static CString packagedInjectedBundleDirectory()
+{
+    // Keep the injected bundle in the same relocatable AppImage runtime as
+    // the subprocesses. Native packages still use PKGLIBDIR below.
+    CString executablePath = FileSystem::currentExecutablePath();
+    if (executablePath.isNull())
+        return { };
+    GUniquePtr<char> executableDirectory(g_path_get_dirname(executablePath.data()));
+    GUniquePtr<char> bundleDirectory(g_build_filename(executableDirectory.get(), "studyvis-webkit-runtime", "injected-bundle", nullptr));
+    GUniquePtr<char> bundlePath(g_build_filename(bundleDirectory.get(), INJECTED_BUNDLE_FILENAME, nullptr));
+    if (g_file_test(bundlePath.get(), G_FILE_TEST_IS_REGULAR))
+        return bundleDirectory.get();
+    return { };
+}
+
 static const char* injectedBundleDirectory()
 {
+    static NeverDestroyed<CString> packagedDirectory = packagedInjectedBundleDirectory();
+    if (!packagedDirectory.get().isNull())
+        return packagedDirectory.get().data();
+
     const char* bundleDirectory = g_getenv("WEBKIT_INJECTED_BUNDLE_PATH");
     if (bundleDirectory && g_file_test(bundleDirectory, G_FILE_TEST_IS_DIR))
         return bundleDirectory;
diff --git a/Source/WebCore/platform/mediastream/gstreamer/DesktopPortal.cpp b/Source/WebCore/platform/mediastream/gstreamer/DesktopPortal.cpp
--- a/Source/WebCore/platform/mediastream/gstreamer/DesktopPortal.cpp
+++ b/Source/WebCore/platform/mediastream/gstreamer/DesktopPortal.cpp
@@ -298,11 +298,10 @@
     nodeData.caps = adoptGRef(gst_caps_new_empty_simple("video/x-raw"));
     gst_caps_set_features_simple(nodeData.caps.get(), gst_caps_features_new("memory:DMABuf", nullptr));
 
-#if GST_CHECK_VERSION(1, 24, 0)
-    auto fourcc = gst_video_dma_drm_fourcc_from_format(GST_VIDEO_FORMAT_BGRA);
-    auto drmFormat = GMallocString::unsafeAdoptFromUTF8(gst_video_dma_drm_fourcc_to_string(fourcc, 0));
-    gst_caps_set_simple(nodeData.caps.get(), "format", G_TYPE_STRING, "DMA_DRM", "drm-format", G_TYPE_STRING, drmFormat.utf8(), nullptr);
-#endif
+    // #312: the packaged PipeWire 1.0.5 plugin predates DMA_DRM caps, even
+    // with newer GStreamer. Its legacy BGRA path negotiates only implicit or
+    // linear DMA-BUF modifiers; DMA_DRM produces an invalid SPA format.
+    gst_caps_set_simple(nodeData.caps.get(), "format", G_TYPE_STRING, "BGRA", nullptr);
     return nodeData;
 }
 
diff --git a/Source/WebCore/Modules/mediastream/gstreamer/GStreamerMediaEndpoint.cpp b/Source/WebCore/Modules/mediastream/gstreamer/GStreamerMediaEndpoint.cpp
--- a/Source/WebCore/Modules/mediastream/gstreamer/GStreamerMediaEndpoint.cpp
+++ b/Source/WebCore/Modules/mediastream/gstreamer/GStreamerMediaEndpoint.cpp
@@ -559,16 +559,23 @@
 Vector<String> getMediaStreamIdsFromSDPMedia(const GstSDPMedia& media)
 {
     HashSet<String> mediaStreamIdsSet;
+    HashSet<String> legacyStreamIds;
+    bool hasMediaLevelMsid = false;
     for (guint i = 0; i < gst_sdp_media_attributes_len(&media); ++i) {
         const auto attribute = gst_sdp_media_get_attribute(&media, i);
         auto key = CStringView::unsafeFromUTF8(attribute->key);
         if (key == "msid"_s) {
             auto components = String(byteCast<char8_t>(unsafeSpan(attribute->value))).split(' ');
-            if (components.size() < 2)
+            if (components.isEmpty())
                 continue;
-            mediaStreamIdsSet.add(components[0]);
-        }
-        // MSID may also come in ssrc attributes, specially if they're in an SDP answer. They look like:
+            hasMediaLevelMsid = true;
+            if (components[0] != "-"_s)
+                mediaStreamIdsSet.add(components[0]);
+        }
+        // Older peers only signal stream IDs in SSRC attributes. Use these
+        // only when media-level MSID is absent: GStreamer can also advertise
+        // a synthetic CNAME here, which is not another MediaStream (RFC 8830).
+        // Legacy attributes look like:
         // a=ssrc:3612593434 msid:e1019f4a-0983-4863-b923-b75903cced2c webrtctransceiver1
         if (key == "ssrc"_s) {
             auto outerComponents = String::fromUTF8(attribute->value).split(' ');
@@ -576,14 +583,15 @@
                 auto innerComponents = outer.split(':');
                 if (innerComponents.size() < 2)
                     continue;
-                if (innerComponents[0] == "msid"_s)
-                    mediaStreamIdsSet.add(innerComponents[1]);
+                if (innerComponents[0] == "msid"_s && innerComponents[1] != "-"_s)
+                    legacyStreamIds.add(innerComponents[1]);
             }
         }
     }
+    const auto& streamIds = hasMediaLevelMsid ? mediaStreamIdsSet : legacyStreamIds;
     Vector<String> mediaStreamIds;
-    mediaStreamIds.reserveInitialCapacity(mediaStreamIdsSet.size());
-    for (const auto& msid : mediaStreamIdsSet)
+    mediaStreamIds.reserveInitialCapacity(streamIds.size());
+    for (const auto& msid : streamIds)
         mediaStreamIds.append(msid);
     return mediaStreamIds;
 }
@@ -1321,44 +1329,50 @@
     onNegotiationNeeded();
 }
 
-void GStreamerMediaEndpoint::recycleTransceiverForSenderTrack(GStreamerRtpTransceiverBackend* transceiverBackend, MediaStreamTrack& track, const FixedVector<String>& mediaStreamIds)
-{
-    auto mediaStreamId = mediaStreamIds.isEmpty() ? emptyString() : mediaStreamIds[0];
-
+void GStreamerMediaEndpoint::recycleTransceiverForSenderTrack(GStreamerRtpTransceiverBackend* transceiverBackend, GStreamerRtpSenderBackend& senderBackend, MediaStreamTrack& track, const FixedVector<String>& mediaStreamIds)
+{
+    auto mediaStreamId = mediaStreamIds.isEmpty() ? "-"_s : mediaStreamIds[0];
     auto transceiver = transceiverBackend->rtcTransceiver();
-    GST_DEBUG_OBJECT(m_pipeline.get(), "Recycling transceiver %" GST_PTR_FORMAT " for track %s", transceiver,  track.id().utf8().data());
-
-    unsigned mLineIndex;
-    g_object_get(transceiver, "mlineindex", &mLineIndex, nullptr);
-
-    GUniqueOutPtr<GstWebRTCSessionDescription> remoteDescription;
-    g_object_get(m_webrtcBin.get(), "remote-description", &remoteDescription.outPtr(), nullptr);
-    if (!remoteDescription)
-        return;
-
-    const auto media = gst_sdp_message_get_media(remoteDescription->sdp, mLineIndex);
-    auto allowedCaps = capsFromSDPMedia(media);
-
-    if (track.privateTrack().isAudio()) {
-        GST_DEBUG_OBJECT(m_pipeline.get(), "Adding outgoing audio source");
-        auto source = RealtimeOutgoingAudioSourceGStreamer::create(m_ssrcGenerator, mediaStreamId, track);
-        configureSource(source.get(), nullptr);
-
-        source->configure(WTF::move(allowedCaps));
-        auto rtpCaps = source->rtpCaps();
-        auto sinkPad = requestPad(rtpCaps, source->mediaStreamID());
-        source->setSinkPad(WTF::move(sinkPad));
-    } else {
+    GST_DEBUG_OBJECT(m_pipeline.get(), "Recycling transceiver %" GST_PTR_FORMAT " for track %s", transceiver, track.id().utf8().data());
+
+    GStreamerRtpSenderBackend::Source source;
+    if (track.privateTrack().isAudio())
+        source = RealtimeOutgoingAudioSourceGStreamer::create(m_ssrcGenerator, mediaStreamId, track);
+    else {
         ASSERT(track.privateTrack().isVideo());
-        GST_DEBUG_OBJECT(m_pipeline.get(), "Adding outgoing video source");
-        auto source = RealtimeOutgoingVideoSourceGStreamer::create(m_ssrcGenerator, mediaStreamId, track);
-        configureSource(source.get(), nullptr);
-
-        source->configure(WTF::move(allowedCaps));
-        auto rtpCaps = source->rtpCaps();
-        auto sinkPad = requestPad(rtpCaps, source->mediaStreamID());
-        source->setSinkPad(WTF::move(sinkPad));
-    }
+        source = RealtimeOutgoingVideoSourceGStreamer::create(m_ssrcGenerator, mediaStreamId, track);
+    }
+
+    auto configureRecycledSource = [&](auto& outgoingSource) {
+        outgoingSource->setRtpHeaderExtensionMapping(m_rtpHeaderExtensions);
+        configureSource(outgoingSource.get(), nullptr);
+
+        // Preserve negotiated codecs while advertising this sender's stream
+        // and track, rather than the remote receiver's previous MSID.
+        GRefPtr<GstCaps> codecPreferences;
+        g_object_get(transceiver, "codec-preferences", &codecPreferences.outPtr(), nullptr);
+        if (!codecPreferences || gst_caps_is_empty(codecPreferences.get()) || gst_caps_is_any(codecPreferences.get()))
+            codecPreferences = adoptGRef(gst_caps_copy(outgoingSource->allowedCaps().get()));
+        else
+            codecPreferences = adoptGRef(gst_caps_make_writable(codecPreferences.leakRef()));
+        auto msid = makeString(mediaStreamId, ' ', track.id());
+        for (unsigned i = 0; i < gst_caps_get_size(codecPreferences.get()); ++i)
+            gst_structure_remove_field(gst_caps_get_structure(codecPreferences.get(), i), "a-msid");
+        gst_structure_set(gst_caps_get_structure(codecPreferences.get(), 0), "a-msid", G_TYPE_STRING, msid.utf8().data(), nullptr);
+        g_object_set(transceiver, "codec-preferences", codecPreferences.get(), nullptr);
+
+        // Use the same deferred negotiation/link/start path as a new sender.
+        // The transceiver may still be recvonly or have no negotiated m-line.
+        m_unlinkedOutgoingSources.append(outgoingSource.ptr());
+    };
+    switchOn(source, [&](Ref<RealtimeOutgoingAudioSourceGStreamer>& outgoingSource) {
+        configureRecycledSource(outgoingSource);
+    }, [&](Ref<RealtimeOutgoingVideoSourceGStreamer>& outgoingSource) {
+        configureRecycledSource(outgoingSource);
+    }, [](std::nullptr_t&) {
+        ASSERT_NOT_REACHED();
+    });
+    senderBackend.setSource(WTF::move(source));
 }
 
 void GStreamerMediaEndpoint::doCreateOffer(const RTCOfferOptions& options)
diff --git a/Source/WebCore/Modules/mediastream/gstreamer/GStreamerMediaEndpoint.h b/Source/WebCore/Modules/mediastream/gstreamer/GStreamerMediaEndpoint.h
--- a/Source/WebCore/Modules/mediastream/gstreamer/GStreamerMediaEndpoint.h
+++ b/Source/WebCore/Modules/mediastream/gstreamer/GStreamerMediaEndpoint.h
@@ -91,7 +91,7 @@
     ExceptionOr<RefPtr<GStreamerRtpSenderBackend>> addTrack(MediaStreamTrack&, const FixedVector<String>&);
     void removeTrack(GStreamerRtpSenderBackend&);
 
-    void recycleTransceiverForSenderTrack(GStreamerRtpTransceiverBackend*, MediaStreamTrack&, const FixedVector<String>&);
+    void recycleTransceiverForSenderTrack(GStreamerRtpTransceiverBackend*, GStreamerRtpSenderBackend&, MediaStreamTrack&, const FixedVector<String>&);
 
     struct Backends {
         RefPtr<GStreamerRtpSenderBackend> senderBackend;
diff --git a/Source/WebCore/Modules/mediastream/gstreamer/GStreamerPeerConnectionBackend.cpp b/Source/WebCore/Modules/mediastream/gstreamer/GStreamerPeerConnectionBackend.cpp
--- a/Source/WebCore/Modules/mediastream/gstreamer/GStreamerPeerConnectionBackend.cpp
+++ b/Source/WebCore/Modules/mediastream/gstreamer/GStreamerPeerConnectionBackend.cpp
@@ -333,7 +333,7 @@
         if (!transceiver)
             return Exception { ExceptionCode::TypeError, "Unable to add track"_s };
 
-        m_endpoint->recycleTransceiverForSenderTrack(reinterpret_cast<GStreamerRtpTransceiverBackend*>(transceiver->backend()), track, mediaStreamIds);
+        m_endpoint->recycleTransceiverForSenderTrack(reinterpret_cast<GStreamerRtpTransceiverBackend*>(transceiver->backend()), backendFromRTPSender(*sender), track, mediaStreamIds);
 
         // 5. If transceiver.[[Direction]] is "recvonly", set transceiver.[[Direction]] to "sendrecv".
         // 6. If transceiver.[[Direction]] is "inactive", set transceiver.[[Direction]] to "sendonly".
